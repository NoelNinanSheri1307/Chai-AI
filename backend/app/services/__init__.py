"""Business services (use-case) layer.

Services orchestrate repositories, the pipeline and external clients on behalf
of HTTP handlers. None are implemented within the foundation milestone; the
modules below are named extension points that will be filled by their
respective milestones.
"""
