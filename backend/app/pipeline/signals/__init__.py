"""Forensic signal extractors.

Each signal module turns a normalized image into a typed signal result
(score, evidence, confidence, severity). The extractors are implemented by the
AI pipeline milestone (Milestone 7), reusing algorithms from the reference
backend's ``layers`` where appropriate.
"""
