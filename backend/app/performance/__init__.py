"""Operational tooling for the Chai AI backend.

Performance profiling (``app.performance.profile``) and deterministic forensic
regression fingerprinting (``app.performance.fingerprints``) live here. This
package is deliberately small pure tooling: it only *measures and compares*
the existing forensic framework and never changes its behaviour.
"""
