"""Reports router: PDF and share-text generation.

Endpoints for ``GET /v1/reports/{analysisId}.pdf`` and share-text arrive with
the reports milestone (Milestone 10). Reserved as an extension point.
"""
