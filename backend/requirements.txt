# Chai AI backend — runtime dependencies.
# Install with:  pip install -r requirements.txt
# (Run from the backend/ directory, with your virtual environment active.)
# Versions are expressed as bounds; the installed environment is pinned by the
# already-resolved set actually used for development and local verification.

# Web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9,<1.0

# Settings & validation
pydantic-settings>=2.3,<3.0

# Persistence / database
sqlmodel>=0.0.24,<0.1
alembic>=1.13,<2.0
psycopg[binary]>=3.2,<4.0

# Forensic detectors (image signal processing)
numpy>=1.24,<2.0
opencv-python-headless>=4.8,<5.0
Pillow>=10.0,<13.0