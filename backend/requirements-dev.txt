# Chai AI backend — development / testing / lint tooling.
# Install with:  pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

httpx>=0.27,<1.0
pytest>=8.0,<9.0
ruff>=0.5,<1.0