# Chai AI — Backend

The backend for Chai AI is not implemented yet. It will be built later.

It is expected to provide the AI forensic analysis API (e.g. `POST /analyze`) that the frontend (`frontend/`) calls, along with any future authentication and data services. No backend code exists in this repository at this time.
